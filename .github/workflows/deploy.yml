name: Deploy to Production

on:
  push:
    branches: [master]

jobs:
  deploy:
    name: Deploy to Hetzner VPS
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: root
          password: ${{ secrets.SERVER_PASSWORD }}
          script_stop: true
          script: |
            cd /opt/influenceurs-tracker

            # Pull latest code
            git fetch origin master
            git reset --hard origin/master

            # Rebuild and restart containers
            docker compose build --no-cache app queue scheduler frontend
            docker compose up -d --force-recreate

            # Clear Laravel caches
            docker exec inf-app php artisan optimize:clear
            docker exec inf-app php artisan optimize

            # Run migrations
            docker exec inf-app php artisan migrate --force

            echo "Deploy complete: $(date)"
